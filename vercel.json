{
  "buildCommand": "corepack yarn nx build dashboard --configuration=production",
  "outputDirectory": "dist/apps/dashboard",
  "installCommand": "corepack yarn install --immutable",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
